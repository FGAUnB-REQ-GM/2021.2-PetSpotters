import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  perfilText: {
    fontSize: 30,
    color: "#B66C6C",
    fontWeight: "bold"
  },
  img: {
    resizeMode: "center",
    position: "absolute",
    borderRadius: 100,
    borderColor: "#000",
  }
})

export default styles