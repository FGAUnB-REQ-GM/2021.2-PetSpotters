import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  logo: {
    marginTop: "10%"
  },
  perfilText: {
    fontSize: 30,
    color: "#B66C6C",
    fontWeight: "bold"
  },
  img: {
    resizeMode: "center",
    position: "absolute",
    borderRadius: 100,
    borderColor: "#000",
  }
})

export default styles